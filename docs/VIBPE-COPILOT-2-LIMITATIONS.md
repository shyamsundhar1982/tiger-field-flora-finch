# VIBPE Co-Pilot 2.0 Current Limitations

The new modules are implemented but are not yet the production entry point. Until `src/lib/ibpe-copilot.ts` is wired to `runVibpeCopilot2` and validated, deployed behavior can continue to use the legacy generic deterministic fallback.
