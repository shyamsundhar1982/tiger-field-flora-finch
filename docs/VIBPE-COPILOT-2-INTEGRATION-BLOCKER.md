# Integration blocker

The only remaining blocker before VIBPE Co-Pilot 2.0 can become active is wiring the new orchestration into `src/lib/ibpe-copilot.ts` and validating the repository build/tests. Until then the deployed application may continue using the legacy fallback behavior.
