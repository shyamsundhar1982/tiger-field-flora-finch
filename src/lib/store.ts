import { create } from "zustand";
import { persist } from "zustand/middleware";
import type { FinanceAssumptions, ProductLineId, ScenarioId } from "@/lib/finance/model";
import { DEFAULT_FINANCE_ASSUMPTIONS } from "@/lib/finance/model";
import { ACTIONS } from "@/lib/data/actions";
type ActionState=Record<string,"open"|"doing"|"done">; const initialActions:ActionState=Object.fromEntries(ACTIONS.map(a=>[a.id,"open"]));
type Store={scenario:ScenarioId;drawStandby:boolean;actions:ActionState;finance:FinanceAssumptions;setScenario:(s:ScenarioId)=>void;setDrawStandby:(v:boolean)=>void;setAction:(id:string,s:"open"|"doing"|"done")=>void;setFinance:(finance:FinanceAssumptions)=>void;updateGlobalFinance:(key:keyof Omit<FinanceAssumptions,"productLines"|"aluminiumVertical">,value:number)=>void;updateProductLine:(id:ProductLineId,key:"aspLakh"|"cogsLakh"|"mixPct"|"launchMonth",value:number)=>void;updateAluminiumVertical:(key:keyof FinanceAssumptions["aluminiumVertical"],value:number)=>void;resetFinance:()=>void};
export const useVeloxis=create<Store>()(persist((set)=>({scenario:"base",drawStandby:true,actions:initialActions,finance:DEFAULT_FINANCE_ASSUMPTIONS,setScenario:scenario=>set({scenario}),setDrawStandby:drawStandby=>set({drawStandby}),setAction:(id,s)=>set(st=>({actions:{...st.actions,[id]:s}})),setFinance:finance=>set({finance}),updateGlobalFinance:(key,value)=>set(st=>({finance:{...st.finance,[key]:value}})),updateProductLine:(id,key,value)=>set(st=>({finance:{...st.finance,productLines:st.finance.productLines.map(line=>line.id===id?{...line,[key]:value}:line)}})),updateAluminiumVertical:(key,value)=>set(st=>({finance:{...st.finance,aluminiumVertical:{...st.finance.aluminiumVertical,[key]:value}}})),resetFinance:()=>set({finance:DEFAULT_FINANCE_ASSUMPTIONS})}),{name:"veloxis-planning-state",partialize:(state)=>({scenario:state.scenario,drawStandby:state.drawStandby,actions:state.actions,finance:state.finance})}));
