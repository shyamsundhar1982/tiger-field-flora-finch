import { createFileRoute, Link } from "@tanstack/react-router";
import { Panel, Kpi } from "@/components/kpi";
import { inr } from "@/lib/format";
import { getAuthoritativeInventory, getInventoryControlSummary, getInventoryMslWarnings, getInventoryFifoTrace } from "@/lib/inventory-authority";
export const Route=createFileRoute("/command/inventory")({loader:async()=>{const [summary,balances,msl,fifo]=await Promise.all([getInventoryControlSummary(),getAuthoritativeInventory(),getInventoryMslWarnings(),getInventoryFifoTrace()]);return {summary,balances,msl,fifo}},component:Inventory});
function Inventory(){const {summary,balances,msl,fifo}=Route.useLoaderData();return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 space-y-6"><header className="flex items-end justify-between gap-4"><div><p className="text-[10px] uppercase tracking-[0.22em] text-green">Operations · inventory control</p><h1 className="mt-2 text-4xl font-bold text-accent">Inventory</h1><p className="mt-2 max-w-3xl text-sm text-muted">One operational inventory chain: controlled SKU identity → BOM requirement → posted stock → ledger/FIFO/MSL → production.</p></div><div className="flex gap-2"><Link to="/command/inventory-master" className="rounded-lg border border-accent px-4 py-2.5 text-sm font-semibold text-accent">Manage SKUs</Link><Link to="/command/bom-control" className="rounded-lg border border-border px-4 py-2.5 text-sm">Manage BOM</Link></div></header><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><Kpi label="Authoritative SKUs" value={String(summary.skuCount)}/><Kpi label="Units" value={String(summary.totalUnits)}/><Kpi label="Inventory value" value={inr(summary.inventoryValueInr)}/><Kpi label="FIFO layers" value={String(summary.fifoLayerCount)}/><Kpi label="MSL alerts" value={String(msl.length)}/></div><Panel title="Operational inventory" kicker="No dummy ledger links"><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-4">{[["stock","Stock ledger","Balance + WAC"],["movements","Movement ledger","Posted events"],["fifo","FIFO ledger","Receipt layers"],["msl","MSL ledger","Replenishment" ]].map(([id,t,n])=><Link key={id} to="/command/inventory-ledgers" search={{ledger:id}} className="rounded-xl border border-border p-4 hover:border-accent"><p className="font-semibold">{t}</p><p className="mt-1 text-xs text-muted">{n}</p><span className="mt-4 inline-block text-xs font-semibold text-accent">Open →</span></Link>)}</div></Panel><div className="grid gap-4 md:grid-cols-3"><Panel title="Inventory Master"><p className="text-sm text-muted">Add, submit and approve SKUs. The existing 60+ catalogue can be migrated as controlled drafts from Master Data.</p><Link to="/command/inventory-master" className="mt-3 inline-block text-xs font-semibold text-accent">Open Inventory Master →</Link></Panel><Panel title="BOM Control"><p className="text-sm text-muted">Create BOM revisions and explicit component-to-SKU requirements. Approved mappings feed Component Control.</p><Link to="/command/bom-control" className="mt-3 inline-block text-xs font-semibold text-accent">Open BOM Control →</Link></Panel><Panel title="Opening Balance"><p className="text-sm text-muted">Use the controlled opening workflow to put starting stock into authoritative inventory.</p><Link to="/command/inventory-openings" className="mt-3 inline-block text-xs font-semibold text-accent">Open Opening Balance →</Link></Panel></div><Panel title="Control chain"><div className="grid gap-3 md:grid-cols-6">{[["01","SKU","Identity"],["02","BOM","Requirement"],["03","Mapping","Approved relationship"],["04","Opening / receipt","Posted stock"],["05","Ledgers","Truth + trace"],["06","Production","Requirement / issue"]].map(([n,t,d])=><div key={n} className="rounded-xl border border-border p-4"><span className="text-[10px] text-accent">{n}</span><p className="mt-2 text-sm font-semibold">{t}</p><p className="mt-1 text-xs text-muted">{d}</p></div>)}</div></Panel></main>}
