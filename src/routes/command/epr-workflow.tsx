import { createFileRoute, Link } from "@tanstack/react-router";
import { Panel } from "@/components/kpi";

export const Route=createFileRoute("/command/epr-workflow")({component:EprWorkflow});
const STEPS=[["01","Commercial handoff","Commercial","/command/sales"],["02","Engineering gate","Engineering","/command/engineering"],["03","Tooling + supplier","Supply & Production","/command/manufacturing"],["04","Pilot build","Supply & Production","/command/operations"],["05","Evidence capture","Governance","/command/qa-verification"],["06","EPR execution","Compliance","/command/epr-execution"],["07","Release readiness","Final gate","/command/deployment-readiness"]] as const;
function EprWorkflow(){return <div className="space-y-6"><header><p className="text-[11px] uppercase tracking-[0.22em] text-green">VINDY · Controlled workflow</p><h1 className="mt-1 font-display text-4xl text-accent">EPR Workflow</h1><p className="mt-2 max-w-4xl text-sm leading-6 text-muted">The end-to-end execution path linking the controlled engineering baseline, supply execution, quality evidence, compliance records and release decision.</p></header><Panel title="Controlled workflow" kicker="Canonical workspaces · evidence before release"><div className="space-y-3">{STEPS.map(([n,title,phase,to],i)=><div key={n} className="flex items-stretch gap-3"><div className="flex w-10 shrink-0 items-center justify-center rounded-lg border border-accent/40 text-xs font-semibold text-accent">{n}</div><Link to={to as never} className="flex-1 rounded-lg border border-border p-4 hover:border-accent/50"><div className="flex flex-wrap items-center justify-between gap-2"><p className="text-sm font-semibold text-fg">{title}</p><span className="text-[10px] uppercase tracking-wider text-subtle">{phase}</span></div><p className="mt-1 text-xs text-muted">Open source-of-truth surface →</p></Link>{i<STEPS.length-1&&<div className="absolute"/>}</div>)}</div></Panel><div className="grid gap-4 md:grid-cols-3"><Panel title="Input" kicker="Authoritative records"><p className="text-sm leading-6 text-muted">Engineering baseline, BOM, commercial assumptions, supplier records, inspection records and evidence.</p></Panel><Panel title="Execution" kicker="EPR control"><p className="text-sm leading-6 text-muted">Assign owner → execute → attach evidence → reconcile deviations → update status.</p></Panel><Panel title="Output" kicker="Release decision"><p className="text-sm leading-6 text-muted">Passed evidence pack, controlled configuration and a GO / HOLD / REWORK / REJECT decision.</p></Panel></div><Link to="/command/epr-execution" className="inline-block rounded-lg border border-accent px-4 py-3 text-sm font-semibold text-accent">Open EPR Execution →</Link></div>}
