import { createFileRoute, Link, redirect } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { SiteFooter, SiteHeader } from "@/components/site-header";
import { SEED_INVENTORY, useInventory, type InventoryCategory, type InventoryItem } from "@/lib/data/inventory";
import { getCommandAccess, getCommandRole } from "@/lib/command-access";
import { canAccessRoute } from "@/lib/page-access";
import { inr } from "@/lib/format";

export const Route = createFileRoute("/inventory")({
  beforeLoad: async () => {
    const access = await getCommandAccess();
    if (!access) throw redirect({ to: "/command-login" });
    const role = await getCommandRole();
    if (!canAccessRoute(role, "/inventory")) throw redirect({ to: "/command" });
  },
  component: InventoryPage,
});
type Draft = Omit<InventoryItem, "updatedAt">;
const BASE_CATEGORIES: InventoryCategory[] = ["groupset","wheelset","tyre","handlebar","stem","saddle","thruaxle","bottom-bracket","bottle-cage","tool-pouch","bracket","fastener","colour","gauge"];
const CATEGORY_KEY = "veloxis-inventory-categories-v1";
const blank: Draft = { id:"",category:"groupset",subcategory:"",brand:"",model:"",detail:"",sku:"",priceInr:0,stockQty:0,reorderLevel:2,coreEnabled:false,proEnabled:false,apexEnabled:false,source:"Internal estimate — verify with supplier",notes:"" };
const title = (x: string) => x.replaceAll("-", " ").replace(/\b\w/g, (c) => c.toUpperCase());
const slug = (x: string) => x.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");

function readCategories() {
  if (typeof window === "undefined") return BASE_CATEGORIES;
  try { const saved = JSON.parse(window.localStorage.getItem(CATEGORY_KEY) || "[]") as string[]; return Array.from(new Set([...BASE_CATEGORIES, ...saved.filter(Boolean)])); }
  catch { return BASE_CATEGORIES;
  }
}

function InventoryPage() {
  const [role, setRole] = useState<"admin" | "viewer" | null>(null);
  const [items, setItems] = useInventory(SEED_INVENTORY);
  const [categories, setCategories] = useState<string[]>(BASE_CATEGORIES);
  const [editing, setEditing] = useState<Draft | null>(null);
  const [filter, setFilter] = useState<string>("all");
  const [categoryDraft, setCategoryDraft] = useState("");
  const [categoryOpen, setCategoryOpen] = useState(false);
  useEffect(() => { getCommandRole().then(setRole).catch(() => setRole(null)); }, []);
  useEffect(() => setCategories(readCategories()), []);
  useEffect(() => { const itemCategories = items.map((x) => x.category as string); const next = Array.from(new Set([...BASE_CATEGORIES, ...categories, ...itemCategories])); if (next.length !== categories.length || next.some((x, i) => x !== categories[i])) setCategories(next); }, [items]);
  function addCategory() { const value = categoryDraft.trim(); if (!value) return; const key = slug(value); if (!key || categories.includes(key)) { setCategoryDraft(""); return; } const next = [...categories, key]; setCategories(next); window.localStorage.setItem(CATEGORY_KEY, JSON.stringify(next.filter((x) => !BASE_CATEGORIES.includes(x as InventoryCategory)))); setCategoryDraft(""); }
  const visible = useMemo(() => filter === "all" ? items : items.filter((x) => x.category === filter), [items, filter]);
  const low = items.filter((x) => x.stockQty <= x.reorderLevel).length;
  function save() { if (!editing?.brand.trim() || !editing.model.trim() || !editing.sku.trim()) return; const next = editing.id && items.some((x) => x.id === editing.id) ? items.map((x) => x.id === editing.id ? { ...editing, updatedAt:new Date().toISOString() } : x) : [...items, { ...editing, id:editing.id || crypto.randomUUID(), updatedAt:new Date().toISOString() }]; setItems(next); setEditing(null); }
  function remove(id: string) { if (!window.confirm("Remove this component from inventory and the configurator?")) return; setItems(items.filter((x) => x.id !== id)); }
  const viewer = role === "viewer";
  return <div className="min-h-dvh bg-bg"><SiteHeader /><fieldset disabled={viewer} className="min-w-0 border-0 p-0 m-0"><div className={viewer ? "pointer-events-none select-none opacity-95" : ""}>
    <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><Link to="/range" className="text-sm text-muted hover:text-accent">Range</Link><p className="mt-5 text-[11px] uppercase tracking-[0.22em] text-green">Operations · component control</p><h1 className="mt-2 text-4xl font-bold text-accent">Component inventory</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-muted">One master catalogue for the configurator. Edit price or stock here and the stocked price/availability is immediately reflected in the build dropdowns on this browser.</p></div><div className="flex flex-wrap gap-2"><button type="button" onClick={() => setCategoryOpen(true)} className="rounded-lg border border-border bg-surface px-4 py-2.5 text-sm font-semibold text-fg hover:border-accent hover:text-accent">+ New category</button><button type="button" onClick={() => setEditing({ ...blank, id:crypto.randomUUID() })} className="rounded-lg border border-accent bg-accent px-4 py-2.5 text-sm font-semibold text-bg hover:bg-fg">+ Add component</button></div></div>
    <div className="mt-8 grid gap-3 sm:grid-cols-3"><Stat label="Component models" value={String(items.length)} /><Stat label="Low / reorder" value={String(low)} /><Stat label="Selectable now" value={String(items.filter((x) => x.stockQty > 0 && (x.coreEnabled || x.proEnabled || x.apexEnabled)).length)} /></div>
    <div className="mt-8 flex max-w-full gap-2 overflow-x-auto pb-1">{(["all", ...categories] as const).map((x) => <button key={x} type="button" onClick={() => setFilter(x)} className={`shrink-0 rounded-full border px-3 py-1.5 text-xs font-semibold uppercase tracking-[0.12em] ${filter === x ? "border-accent bg-accent/10 text-accent" : "border-border text-muted hover:border-accent/50"}`}>{x === "all" ? "All" : title(x)}</button>)}</div>
    <div className="mt-5 overflow-x-auto rounded-xl border border-border bg-bg-elevated/70"><table className="w-full min-w-[1100px] text-left text-sm"><thead className="bg-green/5 text-[10px] uppercase tracking-[0.14em] text-green"><tr><th className="px-4 py-3">Component</th><th className="px-4 py-3">SKU</th><th className="px-4 py-3">Price</th><th className="px-4 py-3">Stock</th><th className="px-4 py-3">Allowed range</th><th className="px-4 py-3">Source</th><th className="px-4 py-3">Actions</th></tr></thead><tbody>{visible.map((x) => <tr key={x.id} className="border-t border-border/70"><td className="px-4 py-3"><p className="font-semibold text-fg">{x.brand} {x.model}</p><p className="mt-0.5 text-xs text-muted">{title(x.category)} · {x.subcategory} · {x.detail}</p></td><td className="px-4 py-3 font-mono text-xs text-muted">{x.sku}</td><td className="px-4 py-3 tabular-nums text-fg">{inr(x.priceInr)}</td><td className="px-4 py-3"><span className={x.stockQty <= x.reorderLevel ? "text-warn" : "text-green"}>{x.stockQty}</span><span className="text-xs text-subtle"> / reorder {x.reorderLevel}</span></td><td className="px-4 py-3"><div className="flex gap-1.5 text-[10px] font-bold uppercase tracking-wider">{x.coreEnabled && <span className="rounded border border-border px-1.5 py-1 text-green">Core</span>}{x.proEnabled && <span className="rounded border border-border px-1.5 py-1 text-green">Pro</span>}{x.apexEnabled && <span className="rounded border border-border px-1.5 py-1 text-green">Apex</span>}</div></td><td className="max-w-[240px] px-4 py-3 text-xs text-muted">{x.source}</td><td className="px-4 py-3"><div className="flex gap-3"><button type="button" onClick={() => setEditing({ ...x })} className="text-xs font-semibold text-accent hover:text-fg">Edit</button><button type="button" onClick={() => remove(x.id)} className="text-xs font-semibold text-muted hover:text-warn">Remove</button></div></td></tr>)}</tbody></table></div>
    <div className="mt-6 rounded-xl border border-border bg-bg-elevated/30 p-4 text-xs leading-5 text-muted"><strong className="text-fg">Control rule:</strong> only components with stock above zero and explicit tier eligibility enter a configuration dropdown. Tier eligibility is an intentional product rule, not an automatic consequence of stocking an item.</div><p className="mt-3 text-[11px] text-subtle">Categories and inventory edits are browser-local for now. A future production database can make these shared across staff and customers.</p></main>
    {editing ? <Editor draft={editing} categories={categories} setDraft={setEditing} onCancel={() => setEditing(null)} onSave={save} onNewCategory={() => setCategoryOpen(true)} /> : null}
    {categoryOpen ? <CategoryModal value={categoryDraft} setValue={setCategoryDraft} onAdd={addCategory} onCancel={() => { setCategoryDraft(""); setCategoryOpen(false); }} /> : null}
  </div></fieldset><SiteFooter /></div>;
}
function Editor({ draft, categories, setDraft, onCancel, onSave, onNewCategory }: { draft:Draft; categories:string[]; setDraft:(v:Draft)=>void; onCancel:()=>void; onSave:()=>void; onNewCategory:()=>void }) { const patch = (p:Partial<Draft>) => setDraft({ ...draft, ...p }); return <div className="fixed inset-0 z-50 overflow-y-auto bg-black/70 p-4"><div className="mx-auto my-8 max-w-3xl rounded-2xl border border-border bg-bg p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] uppercase tracking-[0.18em] text-green">Inventory master</p><h2 className="mt-1 text-2xl font-bold text-accent">Component editor</h2></div><button type="button" onClick={onCancel} className="text-muted hover:text-fg">Close</button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Category"><div className="flex gap-2"><select value={draft.category} onChange={(e) => patch({ category:e.target.value as InventoryCategory })} className="control min-w-0 flex-1">{categories.map((c) => <option key={c} value={c}>{title(c)}</option>)}</select><button type="button" onClick={onNewCategory} className="shrink-0 rounded-lg border border-border px-3 text-xs font-semibold text-muted hover:border-accent hover:text-accent">New</button></div></Field><Field label="Sub-category"><input value={draft.subcategory} onChange={(e) => patch({ subcategory:e.target.value })} className="control" /></Field><Field label="Brand"><input value={draft.brand} onChange={(e) => patch({ brand:e.target.value })} className="control" /></Field><Field label="Model"><input value={draft.model} onChange={(e) => patch({ model:e.target.value })} className="control" /></Field><Field label="Detail / size"><input value={draft.detail} onChange={(e) => patch({ detail:e.target.value })} className="control" /></Field><Field label="SKU"><input value={draft.sku} onChange={(e) => patch({ sku:e.target.value })} className="control" /><span className="mt-1 block text-[10px] text-subtle">Keep unique.</span></Field><Field label="Price (₹)"><input type="number" min="0" step="100" value={draft.priceInr} onChange={(e) => patch({ priceInr:Number(e.target.value) })} className="control" /><span className="mt-1 block text-[10px] text-subtle">This is the live configuration price source.</span></Field><Field label="Stock quantity"><input type="number" min="0" value={draft.stockQty} onChange={(e) => patch({ stockQty:Number(e.target.value) })} className="control" /></Field><Field label="Reorder level"><input type="number" min="0" value={draft.reorderLevel} onChange={(e) => patch({ reorderLevel:Number(e.target.value) })} className="control" /></Field><Field label="Source"><input value={draft.source} onChange={(e) => patch({ source:e.target.value })} className="control" /></Field><Field label="Notes"><input value={draft.notes} onChange={(e) => patch({ notes:e.target.value })} className="control" /></Field><Field label="Configurator eligibility"><div className="flex flex-wrap gap-2">{(["coreEnabled","proEnabled","apexEnabled"] as const).map((key) => <label key={key} className="flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-xs font-semibold"><input type="checkbox" checked={draft[key]} onChange={(e) => patch({ [key]:e.target.checked } as Partial<Draft>)} />{key.replace("Enabled","")}</label>)}</div></Field></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={onCancel} className="rounded-lg border border-border px-4 py-2 text-sm text-muted">Cancel</button><button type="button" onClick={onSave} className="rounded-lg bg-accent px-4 py-2 text-sm font-semibold text-bg">Save component</button></div></div></div>; }
function CategoryModal({ value, setValue, onAdd, onCancel }: { value:string; setValue:(v:string)=>void; onAdd:()=>void; onCancel:()=>void }) { return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/70 p-4"><div className="w-full max-w-lg rounded-2xl border border-border bg-bg-elevated p-6 shadow-2xl"><p className="text-[10px] uppercase tracking-[0.18em] text-green">Inventory taxonomy</p><h2 className="mt-1 text-2xl font-bold text-accent">Add a new component category</h2><p className="mt-2 text-sm leading-6 text-muted">Use this when a component family does not fit the existing catalogue. The new category becomes available in the filters and component editor.</p><label className="mt-5 block"><span className="mb-1.5 block text-[10px] font-semibold uppercase tracking-[0.14em] text-subtle">Category name</span><input autoFocus value={value} onChange={(e) => setValue(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") onAdd(); }} placeholder="e.g. Pedals, Seatpost, Sensor" className="control" /></label><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onCancel} className="rounded-lg border border-border px-4 py-2 text-sm text-muted">Cancel</button><button type="button" onClick={() => { onAdd(); onCancel(); }} className="rounded-lg bg-accent px-4 py-2 text-sm font-semibold text-bg">Add category</button></div></div></div>; }
function Field({label,children}:{label:string;children:React.ReactNode}) { return <label className="block"><span className="mb-1.5 block text-[10px] font-semibold uppercase tracking-[0.14em] text-subtle">{label}</span>{children}</label>; }
function Stat({label,value}:{label:string;value:string}) { return <div className="rounded-xl border border-border bg-bg-elevated/40 p-4"><p className="text-[10px] uppercase tracking-[0.14em] text-subtle">{label}</p><p className="mt-1 text-2xl font-bold tabular-nums text-accent">{value}</p></div>; }
